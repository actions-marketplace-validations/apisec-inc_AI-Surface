openapi: 3.0.0
info:
  title: acme-payments
  version: 1.0.0
paths:
  /v1/orders/{id}/refund:
    post:
      summary: Refund an order
      security:
        - bearerAuth: []
  /v1/health:
    get:
      summary: Health check
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
